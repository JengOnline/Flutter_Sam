import 'package:flutter/material.dart';

class Feat2 extends StatelessWidget {
  const Feat2({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("hello it's me"),
      ),
      body: const Center(
        child: Text("i just wondering"),
      ),
    );
  }
}
